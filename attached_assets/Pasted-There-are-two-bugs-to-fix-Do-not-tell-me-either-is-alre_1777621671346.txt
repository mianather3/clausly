There are two bugs to fix. Do not tell me either is already implemented. Show me the code changes you make.
Bug 1 — Generate Document page shows only one Download button that downloads HTML:
After a document is generated, the page shows a single 'Download' button that downloads an HTML file. But when the same document is viewed from the Documents history page, it correctly shows 'Download Word (.docx)' and 'Download PDF' buttons that work properly.
The fix: The document generation result view and the document detail/history view must use the exact same download component with the exact same two buttons — 'Download Word (.docx)' and 'Download PDF'. Find where the generation result is rendered (likely a success state in the Generate Document page) and replace the single Download button with the same two-button component used in DocumentDetailPage. Do not show a separate download button on the generation page at all — instead, after generation succeeds, automatically redirect the user to the Document Detail page for that document where the correct buttons already exist.
Bug 2 — Word and PDF downloads still show 8 underscores instead of 32:
The normalizeSignatureLines function is supposed to fix short underscore lines but it is not working in the downloaded files. The HTML preview shows 32 underscores correctly but Word and PDF do not.
The fix: The normalizeSignatureLines function must be called on the raw content string BEFORE it is passed into buildDocxFromText and BEFORE it is passed into the PDF builder. Show me the exact lines where buildDocxFromText and the PDF builder are called, and confirm normalizeSignatureLines wraps the content at both call sites like this: buildDocxFromText(title, normalizeSignatureLines(content)) and the same for PDF. If it is already wrapped, paste the actual current code so I can see it. Then test by generating a fresh NDA and downloading both Word and PDF and confirming the signature lines show 32 underscores.
Fix Bug 1 first, then Bug 2. Redeploy after both are done.